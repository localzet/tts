FROM python:3.11-slim

WORKDIR /app

# Install Python dependencies
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy cleanup script
COPY cleanup.py .

# Run cleanup every hour
CMD ["sh", "-c", "while true; do python cleanup.py; sleep 3600; done"]

